.container
    if user && user.email
        .row
            .col-md-12
                .well
                    h1 User Logged in!
                    h3 Hello: #{user.email}
                    p.small User Id: #{user.id}
                    p.small Email: #{user.email}
    block content